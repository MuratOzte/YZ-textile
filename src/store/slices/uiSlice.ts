import { createSlice } from '@reduxjs/toolkit';

export interface ui {}

const initialState = {};

const uiSlice = createSlice({
    name: 'ui',
    initialState,
    reducers: {},
});

export default uiSlice;
